name: dargon2

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image:  google/dart:latest
    steps:
    - uses: actions/checkout@v2
    - name: Run tests
      run: |
        cd dargon2
        pub get
        pub run test
